#include "$VERTEXFITROOT/share/jobOptions_VertexDbSvc.txt"
ApplicationMgr.DLLs   += {"JpsiDzeroOmega"};
ApplicationMgr.TopAlg += {"JpsiDzeroOmega"};

// * Define r0, z0 cut for charged tracks *
JpsiDzeroOmega.Vr0cut    = 9999.; // 1.0
JpsiDzeroOmega.Vz0cut    = 9999.; // 5.0
JpsiDzeroOmega.Vrvz0cut  =   10.;
JpsiDzeroOmega.Vrvxy0cut =    1.;

// * Define energy, dphi, dthe cuts for fake gamma's *
JpsiDzeroOmega.EnergyThreshold = 0.04;
JpsiDzeroOmega.GammaPhiCut     = 20.0;
JpsiDzeroOmega.GammaThetaCut   = 20.0;
JpsiDzeroOmega.GammaAngleCut   = 20.0;

// * Define invariant mass window cut *
JpsiDzeroOmega.dM_rho0 = .150;

// * Whether to test the success of the 4- and 5-constraint fits *
JpsiDzeroOmega.Test4C   = true; // write fit4c
JpsiDzeroOmega.Test5C   = true; // write fit5c and geff
JpsiDzeroOmega.MaxChiSq = 200.; // chisq for both fits should be less

// * Whether or not to check success of Particle Identification *
JpsiDzeroOmega.CheckDedx = true;
JpsiDzeroOmega.CheckTof  = true;